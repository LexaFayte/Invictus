/// scr_ship_init(hp, spd, weapon, firespeed)

//state       = 'idle';
//acc         = 0.5; // acceleration
hp          = argument0;
movespeed   = argument1;
weapon      = argument2;

firespeed   = argument3;
alarm[1]    = firespeed
